{
  "name": "crypto-lab-entropy-collapse",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Seed provenance & RNG state duplication — when two machines share a DRBG seed, they share every secret.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:a11y": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
